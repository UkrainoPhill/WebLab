import React from 'react';
import './SearchInput.css';

const SearchInput = () => {
    return (
        <input type="text" placeholder="Type something..." className="input_search"/>
    );
};

export default SearchInput;